# Source from hyprland.conf, or copy these into custom.lua / a plugin {} block.
# After building: cmake -S hyprdeco -B hyprdeco/build && cmake --build hyprdeco/build
#
# plugin = /home/kirra/Projects/hyprkirra/hyprdeco/kirracorners.so
#
# After rebuilding, restart Hyprland, then:
#   hyprctl plugin load /home/kirra/Projects/hyprkirra/hyprdeco/kirracorners.so
#
# Drop the full rectangle so only the L-braces show:
# general:border_size = 0
#
# plugin:kirracorners {
#     enabled = true
#     animate = true
#     ws_zoom = true
#     jump_sec = 0.28
#     ws_zoom_scale = 0.55
#     arm = 14
#     thick = 1.15
#     inset = 3.5
#     line_alpha = 0.82
#     inner_alpha = 0.7
#     col.line = rgba(ff7a18ff)
#     col.dim = rgba(8a4210ff)
# }
